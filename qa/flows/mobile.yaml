# GENERATED by scripts/qa/compile-flow.mjs — DO NOT EDIT.
# Source of truth: qa/journey.json + qa/selectors.json.
# Re-generate:  node scripts/qa/compile-flow.mjs
# Validate:     node scripts/qa/lint-flows.mjs   (fails if this file is stale)
#
# STORE is supplied per-run via --env=STORE=<store>. There is deliberately NO
# flow-level env: block — a baked default out-ranks the CLI flag and writes
# captures to the wrong store dir (hard-won; see runbooks/qa-capture.md).

appId: com.joshapproved.splitexpenses
---
- launchApp:
    clearState: true
- waitForAnimationToEnd:
    timeout: 8000
- takeScreenshot: qa/captures/${STORE}/00-launch
- extendedWaitUntil:
    visible: { text: "Groups" }
    timeout: 30000
- extendedWaitUntil:
    visible: { text: "Lisbon trip.*" }
    timeout: 15000
- takeScreenshot: qa/captures/${STORE}/list
- tapOn:
    text: "Lisbon trip.*"
    retryTapIfNoChange: true
- extendedWaitUntil:
    visible: { text: "Add expense" }
    timeout: 10000
- takeScreenshot: qa/captures/${STORE}/detail
- assertNotVisible: { text: ".*\\$42\\.00.*" }
- tapOn:
    text: "Add expense"
    retryTapIfNoChange: true
- extendedWaitUntil:
    visible: { text: "New expense" }
    timeout: 10000
- tapOn:
    text: "Amount"
    retryTapIfNoChange: true
- inputText: "42"
- tapOn:
    text: "Description"
    retryTapIfNoChange: true
- takeScreenshot: qa/captures/${STORE}/editor
- tapOn:
    text: "Add expense"
    retryTapIfNoChange: true
- extendedWaitUntil:
    visible: { text: "Add expense" }
    timeout: 10000
- assertVisible: { text: ".*\\$42\\.00.*" }
